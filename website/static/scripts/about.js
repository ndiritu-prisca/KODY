// Get the view properties button element
const viewPropertiesButton = document.getElementById("view-properties-button");

// Add an event listener to the button
viewPropertiesButton.addEventListener("click", function() {
  // Redirect to the properties page
  window.location.href = "properties.html";
});